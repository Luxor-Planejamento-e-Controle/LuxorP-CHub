# dados reais gerados pelo tools/build_data.py — NÃO versionar (financeiro/PII).
# Regenerar localmente: python tools/build_data.py
assets/data/
assets/inadimplencia/

# ambientes
.venv/
__pycache__/
*.pyc
.env

# seeds com e-mails reais — a allowlist vive no Supabase, nao no Git
sql/*.local.sql
