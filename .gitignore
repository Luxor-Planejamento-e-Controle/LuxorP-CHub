# ============================================================
# REPO PÚBLICO. Nada de dado real, e-mail ou segredo aqui.
# ============================================================

# snapshots gerados pelo tools/build_data.py (financeiro real)
# regenerar localmente: python tools/build_data.py
assets/data/

# dashboard de inadimplência (PII de devedores) — nunca versionar
assets/inadimplencia/
output_pbi/
*.xlsx
*.xlsm
*.csv
*.parquet
*.pbix

# seeds com e-mails reais — a allowlist vive no Supabase, não no Git
sql/*.local.sql

# segredos
.env
.env.*
!.env.example
*.pem
*.key

# fontes comerciais (licença OurType) — NÃO redistribuir em repo público.
# O site usa subset woff embutido em assets/fonts.css; os arquivos originais
# ficam fora do repo (Downloads\luxor_fontes).
luxor_fontes/
*.otf
*.ttf
*.woff
*.woff2
*.eot

# ambientes
.venv/
__pycache__/
*.pyc
node_modules/
.netlify/

# lixo de sistema
__MACOSX/
.DS_Store
._*
Thumbs.db
desktop.ini
